{
  "scripts": {
    "build-frontend": "cd frontend && yarn build",
    "start-backend": "cd backend && yarn server:prod",
    "start": "npm-run-all build-frontend start-backend"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
  
}
